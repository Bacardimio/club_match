// Vercel Speed Insights initialization
// This script loads and initializes Speed Insights for the application

// Initialize the Speed Insights queue
window.si = window.si || function () { (window.siq = window.siq || []).push(arguments); };

// The actual Speed Insights script will be injected by Vercel's infrastructure
// after deployment when Speed Insights is enabled in the dashboard.
// This creates the necessary tracking interface.
